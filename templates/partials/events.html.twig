<div class="container">
    <div class="row">
        <div class="col-lg-8 mx-auto">
            <h2>Upcoming Photo Walks</h2>
            <p>
                Photo Walks are scheduled every other Wednesday night, each at a different location around Boston. Everyone meets at the starting location, then walks along a designated route stopping for photos along the way.
            </p>
            {% for event in events %}
                <div class="event-card">
                    <div class="event-card--top" style="background-image: url('{{ event.bannerImageUrl }}');">
                        <div class="event-card--top--inner">
                            <div class="event-card--date">
                                <div class="event-card--date--month">
                                    {{event.date|date('M')}}
                                </div>
                                <div class="event-card--date--day">
                                    {{event.date|date('d')}}
                                </div>
                                <div class="event-card--date--year">
                                    {{event.date|date('Y')}}
                                </div>
                            </div>
                            <div class="event-card--top--title">
                                <h2>{{event.title}}</h2>
                            </div>
                            <div class="clearfix"></div>
                        </div>
                    </div>
                    <div class="event-card--bottom">
                        <p>{{event.description}}  </p>
                        <a class="btn btn-info" href="/events/{{event.id}}" role="button">
                            View Details / RSVP
                        </a>
                    </div>
                </div>
            {% endfor %}
            {% if events is empty %}
                <p class="lead">Currently, there are no <strong>publicly</strong> scheduled upcoming events.</p>
                <p>If you are interested in joining non-public upcoming events, send an email to <a href="mailto:join@photowalkwednesdays.com">join@photowalkwednesdays.com</a> and include the following information:</p>
                <ol>
                    <li>Your full name</li>
                    <li>Do you want to photograph or model?</li>
                    <li>Links to your existing work or Instagram</li>
                </ol>
            {% endif %}
        </div>
    </div>
</div>